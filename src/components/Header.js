import React from "react";

const Header = () => {
    return (
        <header className="header">
            <section>
                {/* Banner texts */}
            </section>
        </header>
    );
};

export default Header;